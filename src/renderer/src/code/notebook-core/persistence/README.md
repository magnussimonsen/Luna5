# notebook-core persistence

Utility helpers for serializing and migrating the workspace structure.

- `serialize.ts` provides `toJSON` and `fromJSON`
- `migrate.ts` placeholder for future schema evolution
